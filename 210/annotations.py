from typing import List
def sum_numbers(numbers: List[int]) -> int:
    """Sums numbers

    Args:
        numbers (List[int]): a list of numbers
    
    Raises:
        TypeError: if not all numeric values passed in
    
    Returns: 
        int: sum of numbers
    
    """
    pass