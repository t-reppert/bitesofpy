from typing import List
def sum_numbers(numbers: List[float]) -> int:
    """Sums numbers

    Args:
        numbers (List[float]): a list of numbers
    
    Raises:
        TypeError: if not all numeric values passed in
    
    Returns: 
        int: sum of numbers
    
    """
    pass